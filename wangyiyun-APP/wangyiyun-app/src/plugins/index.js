//vant 集中管理
import { Button, ConfigProvider, Swipe, SwipeItem, List, Popup } from "vant";

let plugins = [Button, ConfigProvider, Swipe, SwipeItem, List, Popup];
// export default function () {
//     var app;
//     for (let key in plugins) {
//         return app.use(key);
//     }
// }
export default plugins;
