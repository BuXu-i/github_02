#include<iostream>
using namespace std;
#include<vector>
int solve(string s) {
	// write code here
	vector<char>arrt;
	vector<int>arr;
	for(int i=0;i<s.size();i++){
		if(s=='('){
			arr.push_back()//弄清楚 四则运算的规则 还是要。。。。。。 
		} 
	}
int mian () {
	freopen("./数据读取.txt","r",stdin);
	string s;
	cin>>s;
	cout<<solve(s)<<endl;
	return 0;
}
